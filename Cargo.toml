[package]
name = "llm-perf"
version = "0.1.16-alpha.0"
edition = "2024"
authors = ["Brian Martin <brayniac@gmail.com>"]
description = "A benchmarking tool for OpenAI-compatible LLM servers"
license = "MIT OR Apache-2.0"

[[bin]]
name = "llm-perf"
path = "src/main.rs"

[dependencies]
anyhow = "1.0"
chrono = { version = "0.4", features = ["serde"] }
clap = { version = "4.4", features = ["derive"] }
arrow = { version = "56", default-features = false, features = ["prettyprint"] }
futures = "0.3"
hf-hub = { version = "0.4", default-features = false, features = ["tokio", "rustls-tls"] }
histogram = "1.1"
humantime = "2.1"
log = "0.4"
metriken = { git = "https://github.com/iopsystems/metriken" }
metriken-exposition = { git = "https://github.com/iopsystems/metriken", features = ["json", "msgpack"] }
num_cpus = "1.16"
parquet = { version = "56", default-features = false, features = ["arrow"] }
rand = "0.8"
regex = "1"
rand_distr = "0.4"
reqwest = { version = "0.12", default-features = false, features = ["json", "stream", "rustls-tls"] }
tracing = "0.1"
tracing-appender = "0.2"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
rmp-serde = "1.3"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tempfile = "3.8"
thiserror = "1.0"
tiktoken-rs = "0.6"
tokio = { version = "1.35", features = ["full"] }
tokio-stream = "0.1"
toml = "0.8"
warp = "0.3"
sysinfo = "0.32"

[dev-dependencies]
mockito = "1.2"
pretty_assertions = "1.4"

[package.metadata.deb]
name = "llm-perf"
maintainer = "Brian Martin <brayniac@gmail.com>"
section = "net"
priority = "optional"
assets = [
    ["target/release/llm-perf", "usr/bin/", "755"],
    ["examples/config.example.toml", "etc/llm-perf/config.toml.example", "644"],
]

[package.metadata.generate-rpm]
name = "llm-perf"
assets = [
    { source = "target/release/llm-perf", dest = "/usr/bin/llm-perf", mode = "0755" },
    { source = "examples/config.example.toml", dest = "/etc/llm-perf/config.toml.example", mode = "0644", config = true },
]

# RPM metadata for spec template
[package.metadata.rpm]
name = "llm-perf"
version = "@VERSION@"
release = "@RELEASE@"
summary = "A benchmarking tool for OpenAI-compatible LLM servers"
license = "MIT OR Apache-2.0"
arch = "@ARCH@"
